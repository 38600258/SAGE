# 架构总览

> 本文件提供项目的顶层架构地图。
> 详细规范参见 `docs/guides/development-standards.md` 第二章。

## 1. 系统架构

<!-- CUSTOMIZE: 填写项目的系统架构图 -->

```
用户请求 → [入口层] → [业务逻辑层] → [数据层]
```

## 2. 目录结构与职责

| 路径 | 类型 | 职责 |
|------|------|------|
| `AGENTS.md` | 导航 | 智能体入口（≤50行），指向各领域文档 |
| `ARCHITECTURE.md` | 文档 | 本文件，顶层架构地图 |
| `CHANGELOG.md` | 文档 | 变更日志（只增不改） |
| `templates/` | 模板 | 任务文档、ADR、PRD 模板（神圣不可侵犯） |
| `docs/guides/` | 规范 | 开发规范、安全规范等 |
| `docs/architecture/` | 架构 | 能力全景图、ADR 归档、已归档任务 |
| `docs/project/` | 项目 | 看板、模式库、决策日志、交接指南 |
| `docs/product/` | 产品 | PRD（产品需求文档） |
| `docs/references/` | 参考 | 业务规则、API 参考、外部资料 |
| `scripts/` | 工具 | 验证脚本、辅助工具 |

## 3. 分层架构

<!-- CUSTOMIZE: 填写项目的分层架构 -->

```
┌──────────────────────────────────────┐
│            UI / API 层               │
├──────────────────────────────────────┤
│           Service 层                 │
├──────────────────────────────────────┤
│          Repository 层               │
├──────────────────────────────────────┤
│         Config / Types 层            │
└──────────────────────────────────────┘
      ↑ 依赖方向：自上而下，禁止反向
```

## 4. 数据流

<!-- CUSTOMIZE: 填写项目的核心数据流 -->

1. **请求入口**: <!-- e.g. HTTP API / 消息队列 / CLI -->
2. **业务处理**: <!-- e.g. Service 层编排 -->
3. **数据持久化**: <!-- e.g. 数据库写入 -->
4. **响应返回**: <!-- e.g. JSON / 消息推送 -->

## 5. 已知局限

<!-- CUSTOMIZE: 填写项目当前的已知局限和技术债 -->

- <!-- e.g. 缺少 XX 功能 -->
- <!-- e.g. XX 模块性能瓶颈 -->

> 详细技术债追踪参见 `docs/project/PROJECT_BOARD.md`
