# 智能体优先的自主编程方法论（Agent-First Autonomous Development Methodology）

> 版本：1.0 | 2026-05-20
> 本方法论工具无关（Tool-Agnostic），适用于任何支持智能体/AI 编程的环境（Antigravity、Cursor、Windsurf、Codex、Claude Code、Copilot Agent 等）。
> 综合来源：六阶段开发流水线实战规范 + OpenAI「在智能体优先的世界中利用 Codex」工程原则。

---

## 一、核心哲学：人类掌舵，智能体执行

在智能体优先的世界中，工程师的核心工作不再是编写代码，而是：

1. **设计环境**——构建智能体能高效工作的结构、约束和反馈回路
2. **明确意图**——将模糊的需求转化为可验证的原子级验收标准
3. **裁决例外**——在智能体遇到无法自主决策的分歧时做出人类判断

**反直觉原则**：当智能体表现不佳时，解决方案几乎从来不是"再努力一点"或"写更长的提示词"。正确的做法是追问："**智能体还缺什么能力、什么结构、什么约束，才能让它自主完成这件事？**"——然后把答案编码到环境中。

---

## 二、三大支柱

### 支柱一：仓库即真相系统（Repository as System of Record）

> 智能体看不到的东西就不存在。

- Slack 讨论、会议记录、人脑中的隐性知识——如果没有被编码到仓库中，智能体就会像迟了三个月入职的新员工一样对其一无所知。
- **所有决策、约束、模式、规范必须以版本化的文本文件（Markdown / 代码 / Schema）存储在仓库中**，而不是散落在聊天记录、网盘或人的记忆里。
- 仓库中的知识必须是**可机器检查的**（覆盖率、新鲜度、交叉链接），否则漂移不可避免。

### 支柱二：约束即加速器（Constraints as Multipliers）

> 对智能体来说，边界不是束缚——是倍增器。

- 智能体在**具有严格边界和可预测结构**的环境中表现最佳。
- **通过强制执行不变量，而非微观管理实现过程**。例如：要求智能体"在边界处验证数据形状"，但不规定用什么库实现。
- 一旦约束被编码（进入检查器、测试、Hook），它就会**立即应用于所有地方**——不像人类规则那样需要反复沟通和遗忘。

### 支柱三：证据驱动，拒绝信任（Evidence Over Trust）

> 不接受"我觉得已经完成了"。

- 智能体的每一个产出（计划、代码、测试、部署）都必须附带**客观可验证的证据**。
- 证据 = 终端输出、测试日志、diff 记录、截图——而不是智能体的自然语言声明。
- 人类对智能体的信任应建立在**可审查的工件（Artifact）**之上，而非对话中的口头承诺。

---

## 三、六阶段开发流水线

这是一套**阶段门控（Stage-Gate）流水线**，每个阶段有明确的进入条件、产出物和退出标准。智能体必须逐阶段推进，严禁跳过或合并。

```
/1 初始化 --> /2 计划盲审（强制） --> /3 编码与测试
                                           |
/6 生产部署 <-- /5 收尾归档 <-- /4 代码盲审（强制）
```

### 任务风险分级（L1 / L2 / L3）

并非所有任务都需要走完完整的六阶段流程。根据任务的影响范围和风险程度，分为三个级别，每个级别对应不同的流程约束：

| 级别 | 触发信号 | 流程差异 | 门禁规则 |
|------|---------|---------|---------|
| **L1 轻量** | 纯文档修改、纯测试补充、纯重构、不涉及外部系统或数据库 | 初始化 → 编码 → 收尾（**跳过**阶段二计划盲审和阶段四代码盲审） | 无额外人工门禁 |
| **L2 标准** | 业务逻辑变更、新增功能、修复 Bug | **完整六阶段**（含强制盲审） | 阶段五（收尾）完成后保留分支等待人类合并 |
| **L3 高危** | 数据库 Schema 变更、外部 API 对接、部署配置变更、涉及敏感数据 | **完整六阶段** + **每个阶段结束时额外人工确认** | 每个阶段结束后智能体必须暂停并等待人类明确放行，严禁自动流转到下一阶段 |

**分级规则**：
- 初始化阶段（阶段一）必须在需求探询完成后确定风险等级并记录在任务文档中。
- **不确定时一律按 L2 处理**，并向用户确认。
- 人类可以在任何时刻将任务升级（如从 L1 升至 L2），但降级需要明确理由。

**L1 的跳过逻辑**：
- L1 跳过盲审不代表没有质量控制——编码阶段的 Red-Green 循环和静态检查仍然强制执行。
- 跳过的仅仅是"独立角色盲审"这一步，因为对于纯文档/测试/重构任务，额外的盲审成本大于收益。

**L3 的门禁机制**：
- L3 任务的每个阶段结束后，智能体不会自动进入下一阶段，而是进入**阻塞（Blocked）**状态。
- 在多智能体环境中：通过 `ask_question` 工具弹出人类决策卡片。
- 在单智能体环境中：智能体输出阶段总结后停止，等待人类输入"继续"或"打回"。
- 这确保了高危变更的每一步都有人类的知情与授权。

---

### 阶段一：初始化（Init）

**目标**：将模糊的需求转化为精确的、可验证的原子级实施计划。

**核心方法**：

1. **苏格拉底式探询（禁止脑补）**
   - 一次只问一个问题。优先选择题（A/B/C），开放题为辅。
   - 即使任务看起来"很简单"也必须走完探询流程。
   - 如发现上下文中存在矛盾，在提问中一并澄清。

2. **多方案对比与推荐**
   - 必须提出 2-3 种技术方案，附带优劣权衡和明确推荐。
   - 优先复用现有能力，而非重新发明轮子。

3. **需求质量门控**
   - 对需求进行定量评估（目标清晰度 / 约束完整度 / 范围界定 / 可测试性），总分不达标则强制停止，要求补充。

4. **原子级任务拆解**
   - 运用第一性原理（解构 → 推演 → 红队攻击），将目标拆解为具体到文件和函数级别的原子步骤。
   - 每个原子步骤有明确的验收标准。

5. **文档即契约**
   - 任务文档（Task Control Plane）是跨阶段、跨会话、跨智能体传递上下文的**唯一桥梁**。
   - 模板必须通过**物理复制**产生，严禁在内存中重建（防止结构漂移）。

6. **L2/L3 任务的扩展章节**
   - 对于 L2/L3 任务，任务文档中的 1.3 节需额外填写两个扩展子节：
     - `1.3a 验收标准`：列出人类可验证的具体行为（如「运行 X 命令输出包含 Y」），L1 可略。
     - `1.3b 风险矩阵`：列出风险项、概率、影响和缓解措施，L1 可略。
   - 这样确保复杂任务有充分的风险和验收约束，同时保持**单一事实来源**——所有计划信息集中在一个任务文档中，不需要维护两个文件。
   - 涉及重大架构取舍时，还需通过物理复制 `ADR-template.md` 生成独立的架构决策记录（ADR）。

7. **产品需求文档（可选输入）**
   - 当用户给出的不是单个任务而是产品级别的需求时，先通过物理复制 `PRD-template.md` 结构化需求，再从 PRD 中拆分出多个独立 Task。
   - PRD 是需求的源头文档，Task 是执行的原子单元。两者通过任务编号交叉引用。

8. **分支隔离**
   - 所有开发必须在独立功能分支上进行。禁止在主干分支上直接编码。

**退出标准**：任务文档 1.1~1.5 节完整填写（L2/L3 含 1.3a 验收标准和 1.3b 风险矩阵） + 功能分支已创建 + 用户已批准设计。

---

### 阶段二：计划盲审（Plan Review）

**目标**：由独立角色（非计划制定者）对计划进行攻击性审查，发现人类和智能体的盲点。

**核心方法**：

1. **盲审隔离**
   - 审查者**不知道计划的制定过程**，只看到产出物。
   - 在多智能体环境中：必须由独立的子代理执行，主代理不得自审自己的计划。
   - 在单智能体环境中：必须切换会话或切换模型，确保审查者没有初始化阶段的记忆。

2. **五维度红队攻击**
   - 逻辑断层：原子步骤间是否有依赖遗漏或顺序错误？
   - 影响范围：可写文件清单是否完整？是否遗漏联动修改？
   - 边界越权：计划是否越出任务边界？
   - 副作用推演：二阶、三阶后果（并发、数据迁移、跨模块回归）
   - 测试覆盖：测试用例是否覆盖核心分支和边界条件？

3. **三级分类输出**
   - ✅ 可直接执行
   - ⚠️ 需修改后执行
   - 🛑 存在阻塞性问题，需要人类决策

**退出标准**：无 🛑 阻塞（有 🛑 则回滚至阶段一重新规划）。

---

### 阶段三：编码与测试（Dev）

**目标**：严格按照经审查的计划实现代码，并生成客观的测试证据。

**核心方法**：

1. **会话卫生**
   - 如果初始化阶段的上下文已经很长，强烈建议开启新会话。任务文档是跨会话的唯一桥梁。

2. **Red-Green 循环（证据先行）**
   - **Red Phase**：先编写失败测试，证明验收标准已建立。必须展示包含 FAIL 的终端输出。
   - **Green Phase**：编写代码让测试通过。必须展示包含 PASSED 的终端输出。
   - 这不是形式主义——**测试输出本身就是不可伪造的证据**。

3. **防漂移自检**
   - 每完成一个原子步骤后，快速确认：当前做的事是否还在计划范围内？如有偏离，立即停止并报告。
   - 这是对抗智能体"好心办坏事"（scope creep）的核心机制。

4. **证据自动化**
   - 理想状态：测试和检查的终端输出应被自动捕获并写入任务文档的证据链，而非由智能体手动复制粘贴。
   - 自动化证据收集消除了伪造和遗漏的可能性。

**退出标准**：所有测试通过 + 静态检查清洁 + 证据链完整填写。

---

### 阶段四：代码盲审（Code Review）

**目标**：由独立角色对编码产出进行深度审查，识别代码质量问题和潜在技术债。

**核心方法**：

1. **盲审隔离**（同阶段二）

2. **八维度质量审查**
   - 正确性：实现是否完整覆盖计划中的所有步骤？
   - 健壮性：错误处理是否完善？是否存在资源泄漏？
   - 一致性：代码风格、命名规范是否统一？
   - 性能：是否引入了隐性的 O(N²) 或不必要的网络调用？
   - 安全性：是否存在硬编码密钥、注入漏洞？
   - 可测试性：测试用例是否只测了 happy path？
   - 证据真实性：审查证据链是否真实、充分，能否支撑结论？
   - 技术债识别：是否有刻意绕过的问题、已知未修复缺陷、留下的 TODO？

3. **技术债前置登记**
   - 审查中发现的技术债务必须在审查阶段就**预填写**到任务文档的遗留问题节中，而不是留到收尾阶段靠弱模型自行判断。

**退出标准**：无 🛑 阻塞（有则回滚至阶段三修正；同一任务打回超 2 次则升级为计划缺陷，回滚至阶段一）。

---

### 阶段五：收尾归档（Finish）

**目标**：确保任务无尾巴地干净关闭——知识沉淀、文档同步、版本提交。

**核心方法**：

1. **技术债搬运**
   - 将任务文档中的遗留问题逐条登记到项目看板的待办事项中，确保问题被持续追踪。

2. **知识蒸馏**
   - 将本次任务中值得跨任务复用的经验沉淀到模式库中：
     - 📗 最佳实践：值得推广的技术模式
     - 📕 反模式与踩坑：已验证的错误路径和正确替代方案
     - 📘 架构决策模式：可复用的架构选择逻辑

3. **任务结算三问**
   - 🚢 规则同步：本次任务是否涉及业务规则的永久性变更？
   - 📜 决策存证：是否有关键的技术方案取舍？
   - 🛡️ 标准沉淀：是否有通用的代码陷阱或最佳实践？

4. **版本管理与归档**
   - 按变更级别（大架构 / 新功能 / Bug修复）进行版本升号。
   - 将任务文档移动归档至历史目录，保持工作区干净。

5. **严禁自动合并**
   - 智能体只能在功能分支上提交。合并到主干的操作必须由人类执行（Pull Request / Merge Request）。
   - 这是人类对代码库的最终守护权。

6. **严禁范围蔓延**
   - 收尾阶段严禁新增功能。如有新需求，必须创建独立任务。

**退出标准**：自检清单全部通过 + 代码已提交到功能分支 + 任务文档已归档。

---

### 阶段六：生产部署（Deploy）

**目标**：安全可靠地将验证通过的代码部署到生产环境。

**核心方法**：

1. **人类授权卡点**
   - 智能体**绝不允许**私自触发发版。必须获得人类的明确祈使句授权。

2. **最终防线检查**
   - 部署前必须进行最后一次完整的测试和静态检查。

3. **红线自检**
   - 在执行部署命令前，必须确认不会将本地数据库或敏感文件上传到生产环境。

4. **部署后验证**
   - 执行健康检查探针，确认服务正常运行。
   - 切回开发分支，准备承接下一个任务。

**退出标准**：部署成功 + 拨测通过 + 已切回开发分支。

---

## 四、反熵机制：对抗智能体的代码腐烂

> 智能体会复现仓库中已存在的模式——包括不好的模式。

随着时间推移，智能体生成的代码会不可避免地引入漂移。以下机制用于持续对抗熵增：

### 4.1 机械化的不变量强制

不要用文档告诉智能体"请遵守分层架构"——**把分层规则写成检查器**。

- 依赖方向检查器：强制业务层按固定方向依赖（如 Types → Config → Repo → Service → UI），跨层反向依赖直接报错。
- 文件大小限制：防止单个文件无限膨胀。
- 命名规范检查：确保命名一致性。

一旦规则被编码，它就**永远生效**。这在人类团队中需要反复沟通和遗忘的东西，对智能体来说只需编码一次。

### 4.2 渐进式情境披露

**给智能体一张地图，而不是一本 1,000 页的说明书。**

- 不要把所有规则塞进一个巨大的指令文件。当一切都"重要"时，一切都不重要了。
- 使用简短的入口文件（50行以内）指向各领域的细分文档。
- 智能体根据当前任务**按需加载**对应的规范文件，避免上下文窗口被无关信息挤占。

### 4.3 周期性垃圾回收

技术债务就像高息贷款——持续以小额偿还远好过让它累积再痛苦地一次性解决。

- 定期（如每天）自动扫描：
  - 文档新鲜度（超过 N 天未更新且代码已变动的文档）
  - 代码中的临时探测代码和硬编码值
  - 模式库条目与当前代码的一致性
- 发现问题后自动发起修复任务。

### 4.4 人类品味的持续注入

智能体生成的代码不总是符合人类的风格偏好——这没关系。只要输出正确、可维护、且对未来的智能体清晰可读，就算达标。

但人类的品味应持续反馈到系统中：
- 审查评论 → 更新开发规范
- 重构 PR → 更新模式库
- Bug → 更新反模式记录

**品味一旦被捕捉，就会通过机械化手段持续应用于每一行代码。**

---

## 五、文档管理体系：智能体的记忆架构

> 对智能体来说，文档不是附属品——它是唯一的长期记忆。
> 代码是智能体的手，文档是智能体的脑。没有结构化的文档体系，智能体就是一个失忆患者。

### 5.1 知识架构总览

仓库中的知识应按以下四层组织，每层有明确的职责边界：

```
                    ┌─────────────────────────────┐
  第一层：导航层     │  AGENTS.md（50行以内的地图）   │
                    │  指向下方各层，不存储细节       │
                    └──────────────┬──────────────┘
                                   │
         ┌─────────────────────────┼─────────────────────────┐
         │                         │                         │
         ▼                         ▼                         ▼
  ┌──────────────┐    ┌──────────────────┐    ┌──────────────────┐
  │ 第二层：规范层 │    │ 第三层：项目状态层 │    │ 第四层：历史归档层 │
  │              │    │                  │    │                  │
  │ 开发规范      │    │ 项目看板          │    │ 已归档任务文档    │
  │ 架构约束      │    │ 活跃任务文档      │    │ 决策日志          │
  │ 安全规则      │    │ 能力全景图        │    │ 变更日志          │
  │ 业务规则      │    │ 交接指南          │    │ 历史架构方案      │
  └──────────────┘    │ 模式库            │    └──────────────────┘
                      └──────────────────┘
```

**核心原则**：智能体从第一层开始，被引导到需要的具体文档。它永远不需要一次性加载全部知识——这就是渐进式披露。

### 5.2 文档分类与职责

| 类别 | 典型文件 | 谁写 | 谁读 | 更新频率 | 生命周期 |
|------|---------|------|------|---------|---------|
| **导航入口** | AGENTS.md | 人类定义，智能体微调 | 智能体（每次启动） | 仅当架构重大变动 | 永久 |
| **开发规范** | development-standards.md | 人类定义，智能体在三问结算时追加 | 智能体（编码时按需加载） | 每次有新的标准沉淀 | 永久，持续演进 |
| **架构文档** | ARCHITECTURE.md, CAPABILITY_SNAPSHOT.md | 智能体生成，人类审定 | 智能体（初始化时加载） | 每次架构变更后 | 永久，持续演进 |
| **业务规则** | references/*.md | 人类维护 | 智能体（涉及业务逻辑时） | 业务规则变更时 | 永久 |
| **项目看板** | PROJECT_BOARD.md | 智能体自动维护 | 人类审阅 + 智能体读取 | 每个任务的开始和结束 | 永久 |
| **活跃任务文档** | ACTIVE_TASK_T-XXX.md | 智能体按阶段填写 | 当前任务的所有参与者 | 每个阶段结束 | 临时（完成后归档） |
| **模式库** | KNOWN_PATTERNS.md | 智能体在收尾阶段蒸馏 | 智能体（初始化时参考） | 每次有新的模式沉淀 | 永久，持续演进 |
| **交接指南** | HANDOVER-GUIDE.md | 智能体更新，人类审定 | 新会话/新智能体启动时 | 重大里程碑后 | 永久，持续演进 |
| **决策日志** | DECISION_LOG.md | 智能体在三问结算时追加 | 智能体（遇到类似决策时） | 每次有关键技术取舍 | 永久，只增不改 |
| **架构决策记录 (ADR)** | ADR-XXX.md | 智能体在初始化阶段生成（涉及架构取舍时） | 智能体（遇到类似架构决策时） | 创建后仅状态可变（已采纳/已废弃） | 永久存档 |
| **产品需求文档 (PRD)** | PRD-XXX.md | 人类定义或智能体辅助结构化 | 智能体（初始化时作为需求源） | 需求变更时 | 永久，持续演进 |
| **变更日志** | CHANGELOG.md | 智能体在收尾阶段生成 | 人类审阅 | 每次发版 | 永久，只增不改 |
| **已归档任务** | tasks/t-xxx.md | 由活跃任务文档归档而来 | 智能体（查阅历史经验时） | 归档后不再修改 | 永久存档 |

### 5.3 文档生命周期管理

每份文档都有明确的生命周期状态，遵循以下流转：

```
[创建] ──> [活跃] ──> [归档] ──> [退役（可选）]
             │
             └── [更新]（在活跃状态下可被反复更新）
```

#### 创建规则

1. **模板驱动创建**：以下文档必须通过**物理复制模板**产生，严禁在内存中重建：
   - `TASK-TEMPLATE.md` → `ACTIVE_TASK_T-XXX.md`（每个任务必须）
   - `ADR-template.md` → `ADR-XXX.md`（涉及架构取舍时）
   - `PRD-template.md` → `PRD-XXX.md`（产品级需求结构化时）
   - **为什么**：智能体在内存中重建模板时，极易丢失章节、改变结构、遗漏元数据。物理复制是唯一可靠的防漂移手段。
   - **验证**：复制后必须进行物理行数校验（行数 >= 模板行数），不通过则拦截。

2. **骨架文件首次创建**：规范类文档（开发规范、模式库等）在项目初始化时由人类或智能体创建骨架结构，后续在阶段五（收尾）中由智能体持续追加内容。

3. **ADR 的特殊生命周期**：ADR 创建后内容冻结，仅允许修改头部的状态字段（`已采纳` → `已废弃`）。如果需要推翻旧决策，创建新的 ADR 并在其中引用旧 ADR 编号。

#### 更新规则

1. **只增不改原则**：决策日志和变更日志只允许在尾部追加新条目，严禁修改历史记录。
2. **阶段内编辑约束**：活跃任务文档中，智能体只能编辑属于当前阶段的章节，严禁篡改其他阶段的内容。
3. **同步更新义务**：当代码变更影响了架构、能力或业务规则时，收尾阶段必须同步更新对应的文档。"三问结算"机制就是强制触发此检查的门控。

#### 归档规则

1. **重命名与移动**：活跃任务文档在收尾后剥离 `ACTIVE_` 前缀，按 kebab-case 重命名（如 `t-608.md`），移入归档目录。
2. **看板同步**：任务在看板中从"进行中"移入"已完成"，填写完成日期。
3. **归档后冻结**：已归档文档不再修改。如需修订历史决策，创建新的决策日志条目指向旧条目并说明变更原因。

#### 退役规则

1. **周期性扫描发现**：由垃圾回收机制发现超过 N 天未被引用且内容与当前代码不一致的文档。
2. **标记而非删除**：在文件头部添加 `[DEPRECATED]` 标记和退役原因，保留历史可追溯性。

### 5.4 渐进式披露架构

**问题**：把所有规则塞进一个巨大的指令文件（如 AGENTS.md）会导致：
- 上下文窗口被无关信息挤占，智能体在当前任务上的表现下降
- 当一切都"重要"时，一切都不重要了——智能体进行局部模式匹配而非有意识导航
- 巨型文件立即腐烂，且难以机械验证

**解决方案**：三级披露架构

| 级别 | 内容 | 加载时机 | 大小限制 |
|------|------|---------|---------|
| T1 导航 | AGENTS.md：项目概述、构建命令、指向 T2 的路径 | 每次会话启动 | 50 行以内 |
| T2 领域 | 各领域规范文件（开发规范、架构文档、安全规则等） | 智能体判断当前任务涉及该领域时按需加载 | 每个文件独立，不超过 500 行 |
| T3 细节 | 具体的 API 参考、Schema 定义、历史任务归档 | 仅在需要精确细节时加载 | 无限制 |

> **命名说明**：文档层级使用 T1/T2/T3（Tier），任务风险使用 L1/L2/L3（Level），两套体系互不混淆。

**实践要求**：
- T1 文件中必须包含清晰的路径指引（如："架构约束详见 `dev/docs/architecture/ARCHITECTURE.md`"）
- 严禁在 T1 文件中堆砌规则细节
- T2 文件应可被独立阅读，不假设读者已经读过其他 T2 文件

### 5.5 文档间交叉引用与索引

1. **索引文件**：每个文档目录应有一个 `index.md`（或由 L1 文件承担索引职责），列出该目录下所有文档的标题、用途和验证状态。

2. **双向引用**：
   - 当文档 A 引用文档 B 时，文档 B 也应标注"被 A 引用"。
   - 这使得在更新 B 时，能快速识别哪些依赖方需要同步检查。

3. **验证状态标记**：每份规范类文档应在头部标注其验证状态：
   - `[CURRENT]`：与代码一致，最近 N 天内有验证
   - `[NEEDS_REVIEW]`：代码已变更但文档未同步
   - `[DEPRECATED]`：已退役

### 5.6 新鲜度与一致性治理

> 过时的文档比没有文档更危险——它会让智能体基于错误的前提做出"正确的"推理。

1. **新鲜度指标**：每份活跃文档应可追踪其最后更新时间。当对应代码变更后超过 N 天文档未更新，触发告警。

2. **一致性校验**：
   - 开发规范中提到的工具版本是否与实际 `pyproject.toml` / `package.json` 一致？
   - 架构文档中描述的模块是否仍然存在于代码目录中？
   - 能力全景图中的条目是否与实际功能匹配？

3. **治理机制**：
   - **自动化校验 (推荐)**：使用通用工作流静态检查器 [sage_linter.py](scripts/sage_linter.py) 强制执行 12 项不变量校验。包含分支隔离、模板守护、日志只增不改、范围锁定和证据链完整性。
   - **最低要求（手动）**：每个任务的收尾阶段强制执行"三问结算"，确保相关文档被同步更新。

#### 静态检查器规则映射表 (12 校验项)

| 校验项 | 校验对象 | 逻辑与拦截标准 |
|------|--------|--------------|
| **1. 模板复制校验** | 任务文档 | 物理行数 ≥ 模板的 80%，且关键标题无缺漏 |
| **2. 任务大纲校验** | 任务文档 | 任务元数据及五个阶段的大标题必须完整存在 |
| **3. 风险扩展校验** | 任务文档 | 当风险等级为 L2/L3 时，验收标准与风险矩阵不得为空且必须有实质内容 |
| **4. 分支隔离校验** | Git 状态 | 当前分支不得为受保护主干（main/dev 等） |
| **5. 规范文档体积** | docs/guides/ | 所有 T2 规范文档行数严格控制在 500 行以内 |
| **6. 文档新鲜扫描** | docs/ | 扫描超过 30 天未更新的非归档活跃文档并发出警告 |
| **7. 模板完整校验** | templates/ | 检测模板文件是否被意外篡改（比对 Git 变动） |
| **8. 日志更新联动** | CHANGELOG.md | 当项目代码发生实际变更时，CHANGELOG.md 必须同步变更 |
| **9. 日志增改限制** | DECISION_LOG.md | 决策日志只允许在末尾追加，检测到删除历史行则阻断 |
| **10. 范围锁定校验** | 任务文档/Git | 限制只能修改 1.4 节所声明的可写源文件，越权修改则阻断 |
| **11. 交叉引用校验** | docs/ | 扫描所有 markdown 文件，确保本地相对路径链接有效 |
| **12. 证据链校验** | 任务文档 | 验证阶段三开发完成后，测试和 lint 验证项是否已打勾确认 |

### 5.7 推荐的目录布局

```
project-root/
├── AGENTS.md                              # T1 导航入口（50行以内）
├── ARCHITECTURE.md                        # 架构顶层地图
├── CHANGELOG.md                           # 变更日志（只增不改）
├── templates/                             # 所有模板（神圣不可侵犯，只允许物理复制使用）
│   ├── TASK-TEMPLATE.md                   # 任务控制面模板（含 L2/L3 扩展章节）
│   ├── ADR-template.md                    # 架构决策记录模板（涉及架构取舍时使用）
│   └── PRD-template.md                    # 产品需求文档模板（产品级需求时使用）
├── docs/
│   ├── guides/
│   │   ├── development-standards.md       # 开发规范（T2）
│   │   └── core-beliefs.md                # 安全/开发设计原则（T2）
│   ├── architecture/
│   │   ├── CAPABILITY_SNAPSHOT.md         # 能力全景图（T2）
│   │   ├── ADR-001.md                     # 架构决策记录（独立归档）
│   │   └── tasks/                         # 已归档任务文档（T3）
│   │       ├── t-601.md
│   │       └── t-608.md
│   ├── project/
│   │   ├── PROJECT_BOARD.md               # 项目看板
│   │   ├── ACTIVE_TASK_T-XXX.md           # 当前活跃任务（临时）
│   │   ├── KNOWN_PATTERNS.md              # 模式库（T2）
│   │   ├── DECISION_LOG.md                # 决策日志（T2，只增不改）
│   │   └── HANDOVER-GUIDE.md              # 交接指南（T2）
│   ├── product/
│   │   └── PRD-XXX.md                     # 产品需求文档（跨任务的需求源头）
│   └── references/
│       └── business-rules-xxx.md          # 业务规则参考（T2）
├── scripts/
│   └── sage_linter.py                    # SAGE 工作流静态检查器 (强制验证 12 个规则)
```

**关键约束**：
- `TASK-TEMPLATE.md` 是神圣的——只允许通过物理复制使用，禁止直接编辑或覆写
- `ACTIVE_TASK_T-XXX.md` 是临时的——任务完成后必须归档移走
- `DECISION_LOG.md` 和 `CHANGELOG.md` 是追加的——禁止修改历史条目
- 所有 T2 文档不超过 500 行——超过则拆分为子文档

---

## 六、跨工具适配指南

本方法论的核心逻辑不依赖任何特定工具。以下是在不同环境中的适配方式：

| 方法论概念 | Antigravity 2.0 | Cursor / Windsurf | Codex CLI | Claude Code | 通用 IDE + CLI |
|-----------|-----------------|-------------------|-----------|-------------|---------------|
| 任务文档 | ACTIVE_TASK.md（Skill 驱动物理拷贝） | 手动拷贝模板文件 | 在仓库中维护 exec-plan | 手动拷贝模板 | 手动拷贝模板 |
| 盲审隔离 | define_subagent 派生独立子代理 | 开新会话/切模型 | 开新 Codex 任务 | 开新对话 | 开新会话 |
| 行为约束 | Rules (rules/*.md, Always-On) | .cursorrules 文件 | AGENTS.md | CLAUDE.md | 项目级配置文件 |
| 物理防护 | Hooks (hooks.json, PostToolUse) | 无原生支持，靠 git hooks | CI 检查 | 无原生支持 | git pre-commit hooks |
| 工作区隔离 | Workspace: 'branch' (Git worktree) | 手动切分支 | 自动 worktree | 手动切分支 | git worktree |
| 渐进式披露 | Skills (SKILL.md, 按需发现加载) | 分文件的 .cursorrules | docs/ 目录 + AGENTS.md 地图 | 分文件指令 | 分文件的 README |
| 周期性扫描 | /schedule Cron | 无原生支持，靠外部 cron | cron + codex CLI | 无原生支持 | cron + 脚本 |
| 证据收集 | run_command 捕获输出 | 终端输出手动复制 | 自动内嵌 | 终端输出手动复制 | 脚本重定向输出 |
| 文档新鲜度 | doc-gardener Skill + /schedule | 手动检查 | doc-gardening agent | 手动检查 | cron + diff 脚本 |

---

## 七、检查清单：你的项目准备好了吗？

在开始使用本方法论前，确认你的项目仓库具备以下基础设施：

**文档体系**：
- [ ] **导航入口**：一个 50 行以内的 AGENTS.md（或等效入口文件），指向各领域细分文档
- [ ] **任务模板**：一个 120+ 行的标准任务文档模板，包含从初始化到收尾的完整章节结构
- [ ] **项目看板**：一个 Markdown 文件或等效工具，用于追踪任务状态和技术债
- [ ] **模式库**：一个记录最佳实践、反模式和架构决策模式的文件
- [ ] **开发规范**：一个定义命名约定、提交格式、代码风格的文件
- [ ] **决策日志**：一个只增不改的关键技术决策记录文件
- [ ] **变更日志**：一个按版本号组织的变更记录文件

**工程基础**：
- [ ] **分支策略**：明确的分支命名规范和合并流程
- [ ] **分层架构检查器**（推荐）：一个能机械验证依赖方向的脚本或 Lint 规则
- [ ] **模板完整性检查**（推荐）：一个验证任务文档物理拷贝完整性的机制

---

## 八、核心信条

1. **智能体遇到困难时，解决方案是改善环境，而不是写更长的提示词。**
2. **能编码为检查器的规则，就不要写成文档。**
3. **不接受没有客观证据的结论。**
4. **合并主干的权力永远属于人类。**
5. **知识如果没有被编码到仓库中，就等于不存在。**
6. **技术债务持续小额偿还，远好过积累后痛苦地一次性解决。**
7. **给智能体一张地图，而不是一本百科全书。**
8. **过时的文档比没有文档更危险。**
9. **模板是神圣的——物理复制，禁止重建。**
