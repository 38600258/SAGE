# 智能体优先的自主编程方法论（Agent-First Autonomous Development Methodology）

> 版本：2.0 | 2026-05-21
> 本方法论为 **Antigravity 2.0 原生适配版**，所有流程均映射到 Antigravity 2.0 的具体工具链。
> 综合来源：六阶段开发流水线实战规范 + OpenAI「在智能体优先的世界中利用 Codex」工程原则。

---

## 一、核心哲学：人类掌舵，智能体执行

在智能体优先的世界中，工程师的核心工作不再是编写代码，而是：

1. **设计环境**——构建智能体能高效工作的结构、约束和反馈回路
2. **明确意图**——将模糊的需求转化为可验证的原子级验收标准
3. **裁决例外**——在智能体遇到无法自主决策的分歧时做出人类判断

**反直觉原则**：当智能体表现不佳时，解决方案几乎从来不是"再努力一点"或"写更长的提示词"。正确的做法是追问："**智能体还缺什么能力、什么结构、什么约束，才能让它自主完成这件事？**"——然后把答案编码到环境中。

---

## 二、三大支柱

### 支柱一：仓库即真相系统（Repository as System of Record）

> 智能体看不到的东西就不存在。

- Slack 讨论、会议记录、人脑中的隐性知识——如果没有被编码到仓库中，智能体就会像迟了三个月入职的新员工一样对其一无所知。
- **所有决策、约束、模式、规范必须以版本化的文本文件（Markdown / 代码 / Schema）存储在仓库中**，而不是散落在聊天记录、网盘或人的记忆里。
- 仓库中的知识必须是**可机器检查的**（覆盖率、新鲜度、交叉链接），否则漂移不可避免。

### 支柱二：约束即加速器（Constraints as Multipliers）

> 对智能体来说，边界不是束缚——是倍增器。

- 智能体在**具有严格边界和可预测结构**的环境中表现最佳。
- **通过强制执行不变量，而非微观管理实现过程**。例如：要求智能体"在边界处验证数据形状"，但不规定用什么库实现。
- 一旦约束被编码（进入检查器、测试、Hook），它就会**立即应用于所有地方**——不像人类规则那样需要反复沟通和遗忘。

### 支柱三：证据驱动，拒绝信任（Evidence Over Trust）

> 不接受"我觉得已经完成了"。

- 智能体的每一个产出（计划、代码、测试、部署）都必须附带**客观可验证的证据**。
- 证据 = 终端输出、测试日志、diff 记录、截图——而不是智能体的自然语言声明。
- 人类对智能体的信任应建立在**可审查的工件（Artifact）**之上，而非对话中的口头承诺。

---

## 三、六阶段开发流水线

这是一套**阶段门控（Stage-Gate）流水线**，每个阶段有明确的进入条件、产出物和退出标准。智能体必须逐阶段推进，严禁跳过或合并。

```
/1 初始化 --> /2 计划盲审（强制） --> /3 编码与测试
                                           |
/6 生产部署 <-- /5 收尾归档 <-- /4 代码盲审（强制）
```

### 任务等级优先原则

进入六阶段前，必须先判定任务等级。L0 免 TASK 文档，由 Main Agent 直接执行机械性最小改动；L1 及以上 TASK 文档 1.1~1.5 填写完成后，即视为冻结输入，只有在冻结输入上才能派发给执行型子代理。

| 等级 | 典型场景 | 最小流程 | 角色分工 |
|------|---------|---------|---------|
| **L0** | 纯机械修正、格式化、极小文案修补 | 主代理直接执行 | 免 TASK 文档、免子代理、免盲审 |
| **L1** | 小范围文档/测试/重构，低风险且边界清晰 | 初始化 → 编码 → 收尾 | 主代理主导，coder 可选 |
| **L2** | 标准功能、流程、门禁、跨文件调整 | 初始化 → 计划盲审 → 编码 → 代码盲审 → 收尾 | 规划保留主线程，coder/closer 可子代理化 |
| **L3** | 架构、安全、数据、部署、高不确定性 | 完整六阶段 + 每阶段人工确认 | 主代理控制流转，子代理仅执行冻结任务 |

**判定规则**：
- 初始化阶段必须先判定任务等级并写入任务文档。
- L1 及以上跨阶段或跨执行载体调用时，任务等级以 TASK 文档元数据为准。
- L1 及以上 TASK 元数据缺失风险等级时，一律按 L2 处理并记录默认决策，禁止乐观降级为 L0/L1。
- L1 及以上任务文档 1.1~1.5 一旦完成，即视为冻结输入，不再由子代理重写。
- 不确定时一律按 L2 处理。

**提示词安放原则**：
- `prompts/` 是 SAGE 角色契约的唯一事实来源，供 Main Agent、CLI、subagent、skill 共同引用。
- skill 只作为工具适配层，负责调用 CLI、创建子代理或拼接上下文；不得复制并分叉角色提示词。
- CLI/subagent 是执行载体，不拥有流程规则；外包审查、编码或收尾时必须传入 TASK 文档和对应 `prompts/*.md`，调度信息从 TASK 元数据读取。
- L0 任务由 Main Agent 直接执行，不创建 TASK、不派发子代理、不触发盲审；仍需最小验证、中文提交和无范围扩张。

**Main Agent 定义**：
- Main Agent 是当前运行的主会话/主工具代理，不是 `prompts/orchestrator.md` 文件。
- orchestrator 是 Main Agent 的职责：判定任务等级、选择流程、加载角色契约、控制阶段流转。
- `prompts/orchestrator.md` 只在流程分级、阶段选择或外包边界不确定时作为参考契约加载，不作为独立执行角色启动。
- planner 的需求澄清、范围取舍和最终计划确认默认保留在 Main Agent 主线程；`prompts/planner.md` 可作为主线程规范或复杂任务的草拟子代理提示词。

**角色行为矩阵**：

| 角色 | L0 | L1 | L2 | L3 |
|------|----|----|----|----|
| planner | 无 TASK 文档，主代理直接执行 | 轻量计划，明确 1.1~1.5 | 完整计划、验收标准、风险矩阵 | L2 全量 + 回滚/授权/人工确认点 |
| reviewer | 不触发盲审 | 默认跳过，调用时轻量抽查 | 强制完整盲审 | 强制高危盲审，OK/WARN 也需人工确认 |
| coder | 主代理直接执行机械性最小改动 | 小范围按计划执行，相关检查 | Red-Green + 完整证据链 | 保守实现，补充风险/回滚验证，完成即停 |
| closer | 主代理直接提交，通常不更新 CHANGELOG | 轻量归档必要验证和文档 | 完整归档、CHANGELOG、知识沉淀判断 | L2 全量 + 残余风险和人工确认 |

### 任务风险分级（L1 / L2 / L3）

并非所有任务都需要走完完整的六阶段流程。根据任务的影响范围和风险程度，分为三个级别：

| 级别 | 触发信号 | 流程差异 | 门禁规则 |
|------|---------|---------|---------|
| **L1 轻量** | 纯文档修改、纯测试补充、纯重构、不涉及外部系统或数据库 | 初始化 → 编码 → 收尾（**跳过**阶段二和阶段四盲审） | 无额外人工门禁 |
| **L2 标准** | 业务逻辑变更、新增功能、修复 Bug | **完整六阶段**（含强制盲审） | 阶段五完成后保留分支等待人类合并 |
| **L3 高危** | 数据库 Schema 变更、外部 API 对接、部署配置变更、涉及敏感数据 | **完整六阶段** + **每个阶段结束时额外人工确认** | 每个阶段结束后智能体必须暂停并等待人类明确放行 |

**分级规则**：
- 初始化阶段必须在需求探询完成后确定风险等级并记录在任务文档中。
- **不确定时一律按 L2 处理**，并向用户确认。
- 人类可以在任何时刻将任务升级，但降级需要明确理由。

**L1 的跳过逻辑**：跳过盲审不代表没有质量控制——编码阶段的 Red-Green 循环和静态检查仍然强制执行。

**L3 的门禁机制**：L3 任务的每个阶段结束后，main agent 进入**阻塞（Blocked）**状态，暂停工具调用并等待人类输入"继续"或"打回"。这确保了高危变更的每一步都有人类的知情与授权。

---

### 阶段一：初始化（Init）

**目标**：将模糊的需求转化为精确的、可验证的原子级实施计划。

**核心方法**：

1. **苏格拉底式探询（禁止脑补）**
   - 一次只问一个问题。优先选择题（A/B/C），开放题为辅。
   - 即使任务看起来"很简单"也必须走完探询流程。
   - 如发现上下文中存在矛盾，在提问中一并澄清。

2. **多方案对比与推荐**
   - 必须提出 2-3 种技术方案，附带优劣权衡和明确推荐。
   - 优先复用现有能力，而非重新发明轮子。

3. **需求质量门控**
   - 对需求进行定量评估（目标清晰度 / 约束完整度 / 范围界定 / 可测试性），总分不达标则强制停止。

4. **原子级任务拆解**
   - 运用第一性原理（解构 → 推演 → 红队攻击），将目标拆解为具体到文件和函数级别的原子步骤。

5. **文档即契约**
   - L1 及以上任务文档（ACTIVE_TASK_T-XXX.md）是跨阶段、跨会话、跨智能体传递上下文的**唯一桥梁**。
   - L1 及以上模板必须通过**物理复制**产生，严禁在内存中重建。
   - L1 及以上交给 coder / closer 等执行型子代理前，TASK 文档 1.1~1.5 必须已完成并冻结。
   - 子代理/CLI 启动时必须读取 TASK 元数据；风险等级缺失时按 L2 执行并记录。

6. **L2/L3 任务的扩展章节**
   - `1.3a 验收标准`：列出人类可验证的具体行为（如「运行 X 命令输出包含 Y」）。
   - `1.3b 风险矩阵`：列出风险项、概率、影响和缓解措施。
   - 涉及重大架构取舍时，还需通过物理复制 `ADR-template.md` 生成独立 ADR。

7. **分支隔离**
   - 所有开发必须在独立功能分支上进行。禁止在主干分支上直接编码。

**Antigravity 实现**：

| 能力 | 实现方式 |
|------|---------|
| 苏格拉底式探询 | `/grill-me` slash command 触发结构化多轮探询 |
| 并行技术调研 | `define_subagent` + `invoke_subagent` 派生 research subagent，Workspace: `inherit`，仅在需要时并行分析代码库和外部文档 |
| 物理复制模板 | L1 及以上执行 `cp templates/TASK-TEMPLATE.md docs/project/ACTIVE_TASK_T-XXX.md` |
| 分支创建 | `run_command` 执行 `git checkout -b feat/T-XXX-description` |

**退出标准**：L0 确认机械性最小改动并记录验证方式；L1 及以上任务文档 1.1~1.5 节完整填写（L2/L3 含验收标准和风险矩阵） + 功能分支已创建 + 用户已批准设计。

> 说明：规划阶段保留主线程，因为它经常需要持续讨论需求边界与取舍；只有在复杂 L2/L3 任务里，才考虑把“方案草拟、代码库调研、检查清单生成”作为辅助子代理能力，而不是默认子代理化。辅助 planner 子代理不得替代 Main Agent 与用户确认需求。

---

### 阶段二：计划盲审（Plan Review）

**目标**：由独立角色（非计划制定者）对计划进行攻击性审查，发现人类和智能体的盲点。

**核心方法**：

1. **盲审隔离**
   - 审查者**不知道计划的制定过程**，只看到产出物。
   - 必须实现模型级和上下文级的双重隔离。

2. **五维度红队攻击**
   - 逻辑断层：原子步骤间是否有依赖遗漏或顺序错误？
   - 影响范围：可写文件清单是否完整？是否遗漏联动修改？
   - 边界越权：计划是否越出任务边界？
   - 副作用推演：二阶、三阶后果（并发、数据迁移、跨模块回归）
   - 测试覆盖：测试用例是否覆盖核心分支和边界条件？

3. **三级分类输出**
   - ✅ 可直接执行
   - ⚠️ 需修改后执行
   - 🛑 存在阻塞性问题，需要人类决策

**CLI 实现**：

通过执行通道配置调用独立 CLI reviewer，达到**双重隔离**：
- **模型级隔离**：CLI 可绑定到不同模型或不同厂商，不依赖主会话配置。
- **上下文级隔离**：CLI 进程完全独立，不携带当前会话的对话历史。

具体命令模板见 `docs/guides/execution-channels.md`。调用时必须显式传入 `REPO_ROOT`、`TASK_PATH`、`ROLE_PROMPT` 和 `PHASE=plan-review`，并要求 reviewer 写回 TASK 2.1 节。

**备选方案（Subagent 盲审）**：当 CLI 不可用时，通过 `define_subagent` 派生审查子代理，注入 `prompts/reviewer.md` 作为系统提示词。此方案实现上下文级隔离但无法实现模型级隔离。

**退出标准**：无 🛑 阻塞（有 🛑 则回滚至阶段一重新规划）。

---

### 阶段三：编码与测试（Dev）

**目标**：严格按照经审查的计划实现代码，并生成客观的测试证据。

**核心方法**：

1. **会话卫生**
   - 如果初始化阶段的上下文已经很长，强烈建议开启新会话。任务文档是跨会话的唯一桥梁。

2. **Red-Green 循环（证据先行）**
   - **Red Phase**：先编写失败测试，证明验收标准已建立。必须展示包含 FAIL 的终端输出。
   - **Green Phase**：编写代码让测试通过。必须展示包含 PASSED 的终端输出。

3. **防漂移自检**
   - 每完成一个原子步骤后，快速确认：当前做的事是否还在计划范围内？如有偏离，立即停止。

4. **证据自动化**
   - 测试和检查的终端输出应被自动捕获并写入任务文档的证据链。

**Antigravity 实现**：

| 能力 | 实现方式 |
|------|---------|
| 证据自动捕获 | `run_command` 执行测试/lint 命令，输出自动作为证据记录 |
| 进度同步 | 每完成一个原子步骤，将结果写入 TASK 文档 3.1 节（开发日志） |
| 静态检查 | `run_command` 执行 `python scripts/sage_linter.py --check-task docs/project/ACTIVE_TASK_T-XXX.md` |
| 执行边界 | coder 子代理只执行 TASK 冻结后的原子步骤，不改写目标和范围 |

**退出标准**：所有测试通过 + 静态检查清洁 + 证据链完整填写。

---

### 阶段四：代码盲审（Code Review）

**目标**：由独立角色对编码产出进行深度审查，识别代码质量问题和潜在技术债。

**核心方法**：

1. **盲审隔离**（同阶段二）

2. **八维度质量审查**
   - 正确性 / 健壮性 / 一致性 / 性能 / 安全性 / 可测试性 / 证据真实性 / 技术债识别

3. **技术债前置登记**
   - 审查中发现的技术债务必须在审查阶段就**预填写**到任务文档的遗留问题节中。

**CLI 实现**：

同阶段二的 CLI 方案，但 `PHASE=code-review`，审查对象加入代码 diff、TASK 3.2 证据链和 lint/test 输出。具体命令模板见 `docs/guides/execution-channels.md`。

**退出标准**：无 🛑 阻塞（有则回滚至阶段三修正；同一任务打回超 2 次则升级为计划缺陷，回滚至阶段一）。

---

### 阶段五：收尾归档（Finish）

**目标**：确保任务无尾巴地干净关闭——知识沉淀、文档同步、版本提交。

**核心方法**：

1. **技术债搬运**：将任务文档中的遗留问题逐条登记到项目看板。
2. **知识蒸馏**：将可复用经验沉淀到模式库（📗 最佳实践 / 📕 反模式 / 📘 架构决策模式）。
3. **任务结算三问**：🚢 规则同步 / 📜 决策存证 / 🛡️ 标准沉淀。
4. **版本管理与归档**：版本升号 + 任务文档移入归档目录。
5. **严禁自动合并**：智能体只能在功能分支上提交。合并到 `dev` 必须由人类执行。
6. **严禁范围蔓延**：收尾阶段严禁新增功能。

**Antigravity 实现**：

| 能力 | 实现方式 |
|------|---------|
| 知识蒸馏写入 | 直接编辑仓库文件：`KNOWN_PATTERNS.md` + `DECISION_LOG.md` |
| 归档操作 | `run_command` 执行 `mv` + `git add/commit` |
| 最终校验 | `run_command` 执行 `python scripts/sage_linter.py --all` |

**退出标准**：自检清单全部通过 + 代码已提交到功能分支 + 任务文档已归档。

---

### 阶段六：生产部署（Deploy）

**目标**：安全可靠地将验证通过的代码部署到生产环境。

**核心方法**：

1. **人类授权卡点**：智能体**绝不允许**私自触发发版。必须获得人类的明确祈使句授权。
2. **最终防线检查**：部署前进行最后一次完整的测试和静态检查。
3. **红线自检**：确认不会将本地数据库或敏感文件上传到生产环境。
4. **部署后验证**：执行健康检查探针，确认服务正常运行。

**Antigravity 实现**：

| 能力 | 实现方式 |
|------|---------|
| 人类授权 | `ask_permission` 工具弹出授权卡片，人类必须明确批准 |
| 物理防护 | `hooks.json` 中配置 `PostToolUse` Hook，拦截未经授权的部署命令（如 `deploy`、`publish`） |
| 部署执行 | 人类批准后，`run_command` 执行部署脚本 |
| 健康检查 | `run_command` 执行探针脚本验证服务状态 |

**退出标准**：部署成功 + 拨测通过 + 已切回开发分支。

---

## 四、反熵机制：对抗智能体的代码腐烂

> 智能体会复现仓库中已存在的模式——包括不好的模式。

### 4.1 机械化的不变量强制

不要用文档告诉智能体"请遵守分层架构"——**把分层规则写成检查器**。

- 依赖方向检查器、文件大小限制、命名规范检查。
- 一旦规则被编码，它就**永远生效**。

**Antigravity 实现**：通过 `run_command` 执行 `sage_linter.py`：

```bash
# 全量校验
python scripts/sage_linter.py --all

# 特定任务校验
python scripts/sage_linter.py --check-task docs/project/ACTIVE_TASK_T-XXX.md
```

### 4.2 渐进式情境披露

**给智能体一张地图，而不是一本 1,000 页的说明书。**

**Antigravity 实现**：`AGENTS.md` 作为 T1 导航入口，Antigravity 启动时自动加载。T1 目标是短、清晰、可执行；通用入口目标 ≤80 行，超过 100 行必须拆到 T2，但不得为了压缩行数删除会影响行为的规则。智能体通过 `view_file` 按需读取 T2/T3 层文档。GEMINI.md 优先级高于 AGENTS.md，用于覆盖或补充工具级配置。

### 4.3 周期性垃圾回收

技术债务就像高息贷款——持续以小额偿还远好过让它累积。

**Antigravity 实现**：通过 `schedule` 工具设置定时扫描：

```
# 使用 /schedule 命令或 schedule 工具设置 cron
# 每天上午 9:00 自动扫描文档新鲜度和代码一致性
schedule cron "0 9 * * *" "执行文档新鲜度扫描：
1. run_command: python scripts/sage_linter.py --all
2. 扫描超过 30 天未更新的活跃文档
3. 检查模式库条目与当前代码的一致性
4. 发现问题后自动创建修复任务"
```

也可通过 `/schedule` slash command 在对话中直接设置。

### 4.4 人类品味的持续注入

智能体生成的代码不总是符合人类的风格偏好——只要输出正确、可维护、且对未来的智能体清晰可读，就算达标。

但人类的品味应持续反馈到系统中：
- 审查评论 → 更新开发规范
- 重构 PR → 更新模式库
- Bug → 更新反模式记录

**品味一旦被捕捉，就会通过机械化手段持续应用于每一行代码。**

---

## 五、文档管理体系：智能体的记忆架构

> 对智能体来说，文档不是附属品——它是唯一的长期记忆。

### 5.1 知识架构总览

```
                    ┌─────────────────────────────┐
  第一层：导航层     │  AGENTS.md（短、清晰、可执行）  │
                    │  指向下方各层，不存储细节       │
                    └──────────────┬──────────────┘
                                   │
         ┌─────────────────────────┼─────────────────────────┐
         │                         │                         │
         ▼                         ▼                         ▼
  ┌──────────────┐    ┌──────────────────┐    ┌──────────────────┐
  │ 第二层：规范层 │    │ 第三层：项目状态层 │    │ 第四层：历史归档层 │
  │ 开发规范      │    │ 项目看板          │    │ 已归档任务文档    │
  │ 架构约束      │    │ 活跃任务文档      │    │ 决策日志          │
  │ 安全规则      │    │ 模式库            │    │ 变更日志          │
  └──────────────┘    └──────────────────┘    └──────────────────┘
```

### 5.2 文档分类与职责

| 类别 | 典型文件 | 谁写 | 更新频率 | 生命周期 |
|------|---------|------|---------|---------|
| **导航入口** | AGENTS.md | 人类定义，智能体微调 | 架构重大变动时 | 永久 |
| **开发规范** | development-standards.md | 人类定义，智能体追加 | 新标准沉淀时 | 永久演进 |
| **架构文档** | ARCHITECTURE.md | 智能体生成，人类审定 | 架构变更后 | 永久演进 |
| **活跃任务文档** | ACTIVE_TASK_T-XXX.md | 智能体按阶段填写 | 每阶段结束 | 临时→归档 |
| **模式库** | KNOWN_PATTERNS.md | 智能体在收尾阶段蒸馏 | 新模式沉淀时 | 永久演进 |
| **决策日志** | DECISION_LOG.md | 智能体在三问结算时追加 | 关键技术取舍时 | 永久只增 |
| **变更日志** | CHANGELOG.md | 智能体在收尾阶段生成 | 每次发版 | 永久只增 |

### 5.3 工具展示辅助层

通用 SAGE 流程只要求 TASK 文档作为唯一事实来源。Artifact、plan view、walkthrough 等展示层属于工具专用能力，应在对应工具覆盖文件中定义派生规则，不写入通用角色契约。

### 5.4 渐进式披露架构

| 级别 | 内容 | 加载时机 | 大小限制 |
|------|------|---------|---------:|
| T1 导航 | AGENTS.md：项目概述、构建命令、指向 T2 的路径、关键行为规则 | 每次会话启动自动加载 | 目标 ≤80 行，硬上限 100 行 |
| T2 领域 | 各领域规范文件（开发规范、架构文档等） | `view_file` 按需加载 | ≤500 行/文件 |
| T3 细节 | API 参考、Schema 定义、历史任务归档 | 仅需精确细节时加载 | 无限制 |

> T1 使用 T（Tier），任务风险使用 L（Level），两套体系互不混淆。

### 5.5 文档生命周期管理

- **模板驱动创建**：L1 及以上 TASK/ADR/PRD 必须通过**物理复制模板**产生，复制后行数校验（≥ 模板 80%）
- **只增不改**：决策日志和变更日志只允许尾部追加
- **阶段内编辑约束**：智能体只能编辑属于当前阶段的章节
- **归档规则**：剥离 `ACTIVE_` 前缀，移入归档目录，看板同步
- **退役标记**：在文件头部添加 `[DEPRECATED]` 标记，保留可追溯性

### 5.6 新鲜度与一致性治理

> 过时的文档比没有文档更危险。

- **自动化校验**：`sage_linter.py` 强制执行 12 项不变量校验
- **最低要求**：每个任务的收尾阶段强制执行"三问结算"

| 校验项 | 校验对象 | 拦截标准 |
|------|--------|---------:|
| 模板复制 | 任务文档 | 行数 ≥ 模板 80%，关键标题无缺漏 |
| 任务大纲 | 任务文档 | 元数据及五阶段大标题完整 |
| 风险扩展 | 任务文档 | L2/L3 的验收标准与风险矩阵有实质内容 |
| 分支隔离 | Git 状态 | 不得在受保护主干上 |
| 规范体积 | docs/guides/ | T2 文档 ≤ 500 行 |
| 文档新鲜 | docs/ | 超 30 天未更新的活跃文档告警 |
| 模板完整 | templates/ | 模板文件未被篡改 |
| 日志联动 | CHANGELOG.md | 代码变更时必须同步 |
| 日志增改 | DECISION_LOG.md | 只允许追加，删除则阻断 |
| 范围锁定 | 任务文档/Git | 只能修改 1.4 节声明的可写文件 |
| 交叉引用 | docs/ | 本地相对路径链接有效 |
| 证据链 | 任务文档 | 阶段三完成后测试和 lint 项已确认 |

---

## 六、Antigravity 2.0 原生能力速查表

| 能力 | 工具/命令 | 用法 | 在工作流中的角色 |
|------|----------|------|----------------|
| **Subagent** | `define_subagent` + `invoke_subagent` | 定义角色提示词 + Workspace 模式（inherit/branch/share），派生独立子代理 | 并行调研、盲审（备选）、大任务拆解 |
| **Schedule** | `schedule` 工具 / `/schedule` 命令 | cron 表达式定时触发（如 `0 9 * * *`） | 周期性垃圾回收、文档新鲜度扫描 |
| **Slash Commands** | `/grill-me` `/goal` `/schedule` `/browser` | 对话中直接触发预设行为 | 需求探询、自主执行、定时任务、浏览器 |
| **Hooks** | `hooks.json` PostToolUse | 配置触发条件和拦截动作 | 物理防护：拦截未授权部署、危险操作 |
| **Permissions** | `ask_permission` 工具 | 弹出授权卡片等待人类批准 | 部署授权、高危操作确认 |
| **CLI 执行通道** | 见 `docs/guides/execution-channels.md` | 独立进程调用指定角色 prompt 和 TASK | reviewer / coder / closer |
| **Run Command** | `run_command` | 执行 shell 命令，自动捕获输出 | 测试、lint、部署、证据收集 |
| **配置层级** | AGENTS.md < GEMINI.md | GEMINI.md 优先级更高，可覆盖 AGENTS.md | 工具级配置覆盖 |

### 模型选择矩阵

盲审通过独立调用的 CLI 进程实现了**模型级隔离与上下文隔离的突破**。具体 CLI 和模型由 `docs/guides/execution-channels.md` 配置，避免把工具实现硬编码进角色契约。

| 角色 | 推荐模型 | 选择方式 | 理由 |
|------|---------|---------|------|
| **Orchestrator（编排器）** | Opus / Pro (high) | UI 下拉菜单 | 需要强推理能力进行任务拆解和决策 |
| **Reviewer（审查员）** | 强审查模型 | 执行通道配置 | 跨厂商模型隔离，消除同源偏差 |
| **Coder（编码员）** | 快速编码模型 | 执行通道配置或 UI | 高频编码迭代，速度优先 |
| **Closer（收尾员）** | 快速整理模型 | 执行通道配置或 UI | 结构化归档任务，模式化操作 |
| **Doc-gardener（文档管家）** | Flash | UI 下拉菜单 | 文档扫描和更新，批量操作 |
| **L1 任务全流程** | Flash | UI 下拉菜单 | 轻量任务，无需强推理 |
| **L3 任务全流程** | Opus | UI 下拉菜单 | 高危任务，每步都需深度推理 |

**工作流中的模型切换节奏**：

```
初始化(强推理) → 计划盲审(Reviewer CLI) → 编码(Coder CLI/主会话) → 代码盲审(Reviewer CLI) → 收尾(Closer CLI/主会话)
```

日常开发以快速模型保证速度，在需要深度推理的初始化阶段切换到强模型；reviewer/coder/closer 的具体 CLI 可通过执行通道配置替换。

---

## 七、检查清单：你的项目准备好了吗？

**文档体系**：
- [ ] **导航入口**：一个短、清晰、可执行的 AGENTS.md，目标 ≤80 行，超过 100 行拆到 T2
- [ ] **任务模板**：一个 120+ 行的标准任务文档模板，包含从初始化到收尾的完整章节结构
- [ ] **项目看板**：一个 Markdown 文件，用于追踪任务状态和技术债
- [ ] **模式库**：记录最佳实践、反模式和架构决策模式的文件
- [ ] **开发规范**：定义命名约定、提交格式、代码风格的文件
- [ ] **决策日志**：只增不改的关键技术决策记录文件
- [ ] **变更日志**：按版本号组织的变更记录文件

**工程基础**：
- [ ] **分支策略**：明确的分支命名规范和合并流程
- [ ] **静态检查器**：`sage_linter.py` 或等效的不变量校验脚本
- [ ] **模板完整性检查**：验证任务文档物理拷贝完整性的机制
- [ ] **Hooks 配置**：`hooks.json` 中配置部署拦截等物理防护规则

---

## 八、核心信条

1. **智能体遇到困难时，解决方案是改善环境，而不是写更长的提示词。**
2. **能编码为检查器的规则，就不要写成文档。**
3. **不接受没有客观证据的结论。**
4. **合并主干的权力永远属于人类。**
5. **知识如果没有被编码到仓库中，就等于不存在。**
6. **技术债务持续小额偿还，远好过积累后痛苦地一次性解决。**
7. **给智能体一张地图，而不是一本百科全书。**
8. **过时的文档比没有文档更危险。**
9. **L1 及以上模板是神圣的——物理复制，禁止重建。**
