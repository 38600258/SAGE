# 变更日志规范 (Changelog Standards)

> T2 级硬规则 | 状态：[CURRENT]
> 入口路由 → [AGENTS.md](../entry/AGENTS.md) | 总览 → [development-standards.md](development-standards.md)

---

## 一、适用阶段

- 收尾阶段必须加载本文件。
- 新增独立交付物、流程规则或文档口径变更时，必须更新 `CHANGELOG.md`。
- L0 纯机械修正可不新增版本；若改变规则、目录口径或历史归档，仍需记录。

---

## 二、版本号判定

| 场景 | 是否递增版本号 | 记录方式 |
|------|----------------|----------|
| 新任务交付一个独立功能、修复、流程规则或文档口径修改 | 是 | 新增一个版本条目，标题包含任务号 |
| 同一任务内的计划修正、审查 BLOCK 修复、收尾补丁 | 否 | 合入该任务当前版本条目，并在 TASK 中记录原因 |
| 同一任务执行中发现另一个独立问题，且需要扩大目标或新增交付物 | 是，且应新建/拆分任务 | 新任务使用下一个任务号和下一个版本号 |
| 事后拆分历史堆叠版本 | 按独立变更补正 | 在 `CHANGELOG.md` 注明为回溯补正，不改 Git 历史 |
| 纯错别字、格式、链接修正且不改变事实或规则 | 否 | 合入当前任务或最近相关版本条目 |

---

## 三、标题格式

`CHANGELOG.md` 每个版本条目必须使用二级标题：

```markdown
## [X.Y.Z] 类型图标 Type 中文标题 (T-XXX|L0|Init) - YYYY-MM-DD HH:mm:ss
```

示例：

```markdown
## [0.1.8] 🔧 Chore 归档任务文档并修正路径口径 (L0) - 2026-05-29 14:28:33
## [0.1.7] 📚 Docs 固化 CLI 模型配置与探针结论 (T-005) - 2026-05-29 12:30:55
## [0.1.4] ✨ Feature 新增米家台灯局域网控制演示 (T-002) - 2026-05-29 10:29:29
```

允许的类型：

| 类型 | 适用场景 |
|------|----------|
| `✨ Feature` | 新能力、新脚本、新用户可见功能 |
| `🐛 BugFix` | 缺陷修复 |
| `📚 Docs` | 文档、流程规则、模板口径 |
| `🔧 Chore` | 仓库整理、归档、工具配置 |
| `♻️ Refactor` | 不改变行为的结构调整 |
| `🧪 Test` | 测试补充或测试基础设施 |

---

## 四、条目正文

- 每个版本至少包含 1 条顶层 bullet。
- 顶层 bullet 先概括交付物，再用子 bullet 列关键变化。
- 不要只写“更新文档”“修复问题”这类不可复核描述。
- 历史补正不改 Git 历史，只在新版本或现有条目中说明补正范围。

---

## 五、收尾自检

- [ ] 版本号是否应递增已经判断并记录。
- [ ] 标题包含 `[X.Y.Z]`、类型、中文标题、任务号或 `L0/Init`、完整时间戳。
- [ ] 条目描述能让人类从 CHANGELOG 追到具体任务或提交。
- [ ] TASK `5.4` 的“CHANGELOG 已更新”和“版本号已更新”说明与本文件一致。
